#include <stdio.h>
/**
  * main - Entry point
  * Return: Always 0
*/
int main(void)
{
	for (int n = 65; n > 90; n++)
	{
		putchar(n);
	}
	for (int m = 97; m > 122; m++)
	{
		putchar(m);
	}
	putchar('\n');
	return (0);
}
